import { Flex } from '@cube-dev/ui-kit';

function Pagination() {
  return (
    <Flex justifyContent="space-between">
      <Flex></Flex>
      <Flex></Flex>
    </Flex>
  );
}

export default Pagination;
