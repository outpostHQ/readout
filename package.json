{
    "name": "readout",
    "private": true,
    "version": "0.1.0",
    "description": "",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "dev": "turbo dev",
      "build": "turbo build"
    },
    "devDependencies": {
      "turbo": "^1.6.3"
    }
  }